# URL of the deployed Ask Ari Cloudflare Worker (see worker/README.md).
# Not a secret — it's a public endpoint the browser calls directly.
VITE_ASK_ARI_WORKER_URL=https://ask-ari.your-subdomain.workers.dev
